import { render, screen } from '@testing-library/react'
import {{pascalCase name}} from './{{lowerCase name}}.component'
describe('<{{pascalCase name}} />', () => {
  it('should render {{pascalCase name}}', () => {})
})
